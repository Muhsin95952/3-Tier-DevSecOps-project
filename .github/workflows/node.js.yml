
name: Node.js CICD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v4
      
    - name: setup nodejs
      uses: actions/setup-node@v4
      with:
        node-version: '22'
    - name: Frontend Compilation
      run: | 
        cd client
        find . -name "*.js" -exec node --check {} +
    - name: Backend Compilation
      run: |
        cd api
        find . -name "*.js" -exec node --check {} +
        
  scan:
    name: gitleaks
    runs-on: ubuntu-latest
    needs: compile
    steps:
      - name: GitLeaks code
        uses: actions/checkout@v4
      
      - name: GitLeaks setup
        uses: gitleaks/gitleaks-action@v2
        
      - name: GitLeaks scan client
        run: gitleaks detect --cource ./client --exec-code-1
          
      - name: GitLeaks scan api
        run: gitleaks detect --cource ./api --exec-code-1
          
      
      
